- title "Entidade - #{@being.name}"
%h1
  = "#{@being.name}"


%p
  = link_to "Editar", edit_admin_being_path(@being), :class => "small secondary button"
  = link_to("Apagar", [:admin, @being], :confirm => 'Are you sure?', :method => :delete, :class => "small secondary button") if User.find(current_user.id).has_role? :admin
  = link_to "Ver Todos", admin_beings_path, :class => "small secondary button"
