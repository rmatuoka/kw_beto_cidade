= form_for [:admin, @schooling] do |f|
  .twelve.columns.alert-box.alert
    = f.error_messages
  %p
    = f.label :name, "Nome"
    = f.text_field :name
  %p
    = f.submit "Salvar", :class => "button"
